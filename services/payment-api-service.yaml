apiVersion: v1
kind: Service
metadata:
  name: payment-authorization-api
  namespace: cde-production
spec:
  selector:
    app: payment-authorization-api
  ports:
    - port: 8080
      targetPort: 8080
