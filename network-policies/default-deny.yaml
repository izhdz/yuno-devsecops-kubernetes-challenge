# Default Deny All Connections Policy
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: cde-production
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress